import React from "react"
//import { Link } from "gatsby"
import "./header.css"
//import biopic from "../../images/watercolorplanet.png"

const MobileBio = (props) => {

    return (
        <div className="mobile-bio-main">
           {/* <img src={biopic} className="ml-4 mt-2" style={{ maxWidth: `75px`, maxHeight: `75px`, borderRadius: `50%`, boxShadow: `1px 1px 3px` }} alt="author-pic" />
          <Link to="/"><h4 className="mr-4 mt-4 text-dark">{props.author}</h4></Link> */}
        </div>
    )
}

export default MobileBio